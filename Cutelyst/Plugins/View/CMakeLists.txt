find_package(Grantlee5)
if (Grantlee5_FOUND)
    message(STATUS "PLUGIN: Grantlee 5 templating found.")
    add_subdirectory(Grantlee)
else (Grantlee5_FOUND)
    message(STATUS "PLUGIN: Not building with Grantlee template plugin.")
endif (Grantlee5_FOUND)

find_library(ClearSilver_CS   libneo_cs.a)
find_library(ClearSilver_Util libneo_utl.a)
if (ClearSilver_CS)
    message(STATUS "PLUGIN: ClearSilver templating found.")
    add_subdirectory(ClearSilver)
else (ClearSilver_CS)
    message(STATUS "PLUGIN: Not building with ClearSilver template plugin.")
endif (ClearSilver_CS)

add_subdirectory(JSON)

find_package(SimpleMailQt5)
if (SimpleMailQt5_FOUND)
    message(STATUS "PLUGIN: View::Email found.")
    add_subdirectory(Email)
else (SimpleMailQt5_FOUND)
    message(STATUS "PLUGIN: Not building with View::Email plugin.")
endif (SimpleMailQt5_FOUND)
