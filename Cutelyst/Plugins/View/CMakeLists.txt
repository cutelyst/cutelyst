find_package(Grantlee5 QUIET)
if (Grantlee5_FOUND)
    message(STATUS "PLUGIN: View::Grantlee, enabled.")
    add_subdirectory(Grantlee)
else (Grantlee5_FOUND)
    message(STATUS "PLUGIN: View::Grantlee, disabled.")
endif (Grantlee5_FOUND)

find_library(ClearSilver_CS   libneo_cs.a)
find_library(ClearSilver_Util libneo_utl.a)
if (ClearSilver_CS)
    message(STATUS "PLUGIN: View::ClearSilver, enabled.")
    add_subdirectory(ClearSilver)
else (ClearSilver_CS)
    message(STATUS "PLUGIN: View::ClearSilver, disabled.")
endif (ClearSilver_CS)

add_subdirectory(JSON)

find_package(SimpleMailQt5 QUIET)
if (SimpleMailQt5_FOUND)
    message(STATUS "PLUGIN: View::Email, enabled.")
    add_subdirectory(Email)
else (SimpleMailQt5_FOUND)
    message(STATUS "PLUGIN: View::Email, disabled.")
endif (SimpleMailQt5_FOUND)
